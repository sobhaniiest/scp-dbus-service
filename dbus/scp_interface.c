/*
 * Generated by gdbus-codegen 2.56.4. DO NOT EDIT.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "scp_interface.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.fedoraproject.Config.Printing
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:scpinterface
 * @title: scpinterface
 * @short_description: Generated C code for the org.fedoraproject.Config.Printing D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-fedoraproject-Config-Printing.top_of_page">org.fedoraproject.Config.Printing</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.fedoraproject.Config.Printing ---- */

static const _ExtendedGDBusArgInfo _scp_interface__method_info_new_printer_dialog_OUT_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_new_printer_dialog_OUT_ARG_pointers[] =
{
  &_scp_interface__method_info_new_printer_dialog_OUT_ARG_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface__method_info_new_printer_dialog =
{
  {
    -1,
    (gchar *) "NewPrinterDialog",
    NULL,
    (GDBusArgInfo **) &_scp_interface__method_info_new_printer_dialog_OUT_ARG_pointers,
    NULL
  },
  "handle-new-printer-dialog",
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_printer_properties_dialog_IN_ARG_xid =
{
  {
    -1,
    (gchar *) "xid",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_printer_properties_dialog_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_printer_properties_dialog_IN_ARG_pointers[] =
{
  &_scp_interface__method_info_printer_properties_dialog_IN_ARG_xid,
  &_scp_interface__method_info_printer_properties_dialog_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_printer_properties_dialog_OUT_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_printer_properties_dialog_OUT_ARG_pointers[] =
{
  &_scp_interface__method_info_printer_properties_dialog_OUT_ARG_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface__method_info_printer_properties_dialog =
{
  {
    -1,
    (gchar *) "PrinterPropertiesDialog",
    (GDBusArgInfo **) &_scp_interface__method_info_printer_properties_dialog_IN_ARG_pointers,
    (GDBusArgInfo **) &_scp_interface__method_info_printer_properties_dialog_OUT_ARG_pointers,
    NULL
  },
  "handle-printer-properties-dialog",
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_job_applet_OUT_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_job_applet_OUT_ARG_pointers[] =
{
  &_scp_interface__method_info_job_applet_OUT_ARG_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface__method_info_job_applet =
{
  {
    -1,
    (gchar *) "JobApplet",
    NULL,
    (GDBusArgInfo **) &_scp_interface__method_info_job_applet_OUT_ARG_pointers,
    NULL
  },
  "handle-job-applet",
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_get_best_drivers_IN_ARG_device_id =
{
  {
    -1,
    (gchar *) "device_id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_get_best_drivers_IN_ARG_device_make_and_model =
{
  {
    -1,
    (gchar *) "device_make_and_model",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_get_best_drivers_IN_ARG_device_uri =
{
  {
    -1,
    (gchar *) "device_uri",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_get_best_drivers_IN_ARG_pointers[] =
{
  &_scp_interface__method_info_get_best_drivers_IN_ARG_device_id,
  &_scp_interface__method_info_get_best_drivers_IN_ARG_device_make_and_model,
  &_scp_interface__method_info_get_best_drivers_IN_ARG_device_uri,
  NULL
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_get_best_drivers_OUT_ARG_drivers =
{
  {
    -1,
    (gchar *) "drivers",
    (gchar *) "a(ss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_get_best_drivers_OUT_ARG_pointers[] =
{
  &_scp_interface__method_info_get_best_drivers_OUT_ARG_drivers,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface__method_info_get_best_drivers =
{
  {
    -1,
    (gchar *) "GetBestDrivers",
    (GDBusArgInfo **) &_scp_interface__method_info_get_best_drivers_IN_ARG_pointers,
    (GDBusArgInfo **) &_scp_interface__method_info_get_best_drivers_OUT_ARG_pointers,
    NULL
  },
  "handle-get-best-drivers",
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_missing_executables_IN_ARG_ppd_filename =
{
  {
    -1,
    (gchar *) "ppd_filename",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_missing_executables_IN_ARG_pointers[] =
{
  &_scp_interface__method_info_missing_executables_IN_ARG_ppd_filename,
  NULL
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_missing_executables_OUT_ARG_missing_executables =
{
  {
    -1,
    (gchar *) "missing_executables",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_missing_executables_OUT_ARG_pointers[] =
{
  &_scp_interface__method_info_missing_executables_OUT_ARG_missing_executables,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface__method_info_missing_executables =
{
  {
    -1,
    (gchar *) "MissingExecutables",
    (GDBusArgInfo **) &_scp_interface__method_info_missing_executables_IN_ARG_pointers,
    (GDBusArgInfo **) &_scp_interface__method_info_missing_executables_OUT_ARG_pointers,
    NULL
  },
  "handle-missing-executables",
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_group_physical_devices_IN_ARG_devices =
{
  {
    -1,
    (gchar *) "devices",
    (gchar *) "a{sa{ss}}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_group_physical_devices_IN_ARG_pointers[] =
{
  &_scp_interface__method_info_group_physical_devices_IN_ARG_devices,
  NULL
};

static const _ExtendedGDBusArgInfo _scp_interface__method_info_group_physical_devices_OUT_ARG_grouped_devices =
{
  {
    -1,
    (gchar *) "grouped_devices",
    (gchar *) "aas",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface__method_info_group_physical_devices_OUT_ARG_pointers[] =
{
  &_scp_interface__method_info_group_physical_devices_OUT_ARG_grouped_devices,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface__method_info_group_physical_devices =
{
  {
    -1,
    (gchar *) "GroupPhysicalDevices",
    (GDBusArgInfo **) &_scp_interface__method_info_group_physical_devices_IN_ARG_pointers,
    (GDBusArgInfo **) &_scp_interface__method_info_group_physical_devices_OUT_ARG_pointers,
    NULL
  },
  "handle-group-physical-devices",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _scp_interface__method_info_pointers[] =
{
  &_scp_interface__method_info_new_printer_dialog,
  &_scp_interface__method_info_printer_properties_dialog,
  &_scp_interface__method_info_job_applet,
  &_scp_interface__method_info_get_best_drivers,
  &_scp_interface__method_info_missing_executables,
  &_scp_interface__method_info_group_physical_devices,
  NULL
};

static const _ExtendedGDBusSignalInfo _scp_interface__signal_info_ready =
{
  {
    -1,
    (gchar *) "ready",
    NULL,
    NULL
  },
  "ready"
};

static const _ExtendedGDBusSignalInfo _scp_interface__signal_info_error =
{
  {
    -1,
    (gchar *) "error",
    NULL,
    NULL
  },
  "error"
};

static const _ExtendedGDBusSignalInfo * const _scp_interface__signal_info_pointers[] =
{
  &_scp_interface__signal_info_ready,
  &_scp_interface__signal_info_error,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _scp_interface__interface_info =
{
  {
    -1,
    (gchar *) "org.fedoraproject.Config.Printing",
    (GDBusMethodInfo **) &_scp_interface__method_info_pointers,
    (GDBusSignalInfo **) &_scp_interface__signal_info_pointers,
    NULL,
    NULL
  },
  "",
};


/**
 * scp_interface__interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-fedoraproject-Config-Printing.top_of_page">org.fedoraproject.Config.Printing</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
scp_interface__interface_info (void)
{
  return (GDBusInterfaceInfo *) &_scp_interface__interface_info.parent_struct;
}

/**
 * scp_interface__override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #scpinterface interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
scp_interface__override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * scpinterface:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing.top_of_page">org.fedoraproject.Config.Printing</link>.
 */

/**
 * scpinterfaceIface:
 * @parent_iface: The parent interface.
 * @handle_get_best_drivers: Handler for the #scpinterface::handle-get-best-drivers signal.
 * @handle_group_physical_devices: Handler for the #scpinterface::handle-group-physical-devices signal.
 * @handle_job_applet: Handler for the #scpinterface::handle-job-applet signal.
 * @handle_missing_executables: Handler for the #scpinterface::handle-missing-executables signal.
 * @handle_new_printer_dialog: Handler for the #scpinterface::handle-new-printer-dialog signal.
 * @handle_printer_properties_dialog: Handler for the #scpinterface::handle-printer-properties-dialog signal.
 * @error: Handler for the #scpinterface::error signal.
 * @ready: Handler for the #scpinterface::ready signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing.top_of_page">org.fedoraproject.Config.Printing</link>.
 */

typedef scpinterfaceIface scpinterfaceInterface;
G_DEFINE_INTERFACE (scpinterface, scp_interface_, G_TYPE_OBJECT)

static void
scp_interface__default_init (scpinterfaceIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * scpinterface::handle-new-printer-dialog:
   * @object: A #scpinterface.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.NewPrinterDialog">NewPrinterDialog()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface__complete_new_printer_dialog() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-new-printer-dialog",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceIface, handle_new_printer_dialog),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * scpinterface::handle-printer-properties-dialog:
   * @object: A #scpinterface.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_xid: Argument passed by remote caller.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.PrinterPropertiesDialog">PrinterPropertiesDialog()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface__complete_printer_properties_dialog() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-printer-properties-dialog",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceIface, handle_printer_properties_dialog),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_STRING);

  /**
   * scpinterface::handle-job-applet:
   * @object: A #scpinterface.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.JobApplet">JobApplet()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface__complete_job_applet() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-job-applet",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceIface, handle_job_applet),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * scpinterface::handle-get-best-drivers:
   * @object: A #scpinterface.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_device_id: Argument passed by remote caller.
   * @arg_device_make_and_model: Argument passed by remote caller.
   * @arg_device_uri: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.GetBestDrivers">GetBestDrivers()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface__complete_get_best_drivers() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-best-drivers",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceIface, handle_get_best_drivers),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * scpinterface::handle-missing-executables:
   * @object: A #scpinterface.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_ppd_filename: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.MissingExecutables">MissingExecutables()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface__complete_missing_executables() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-missing-executables",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceIface, handle_missing_executables),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * scpinterface::handle-group-physical-devices:
   * @object: A #scpinterface.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_devices: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.GroupPhysicalDevices">GroupPhysicalDevices()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface__complete_group_physical_devices() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-group-physical-devices",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceIface, handle_group_physical_devices),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /* GObject signals for received D-Bus signals: */
  /**
   * scpinterface::ready:
   * @object: A #scpinterface.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-fedoraproject-Config-Printing.ready">"ready"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("ready",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceIface, ready),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

  /**
   * scpinterface::error:
   * @object: A #scpinterface.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-fedoraproject-Config-Printing.error">"error"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("error",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceIface, error),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

}

/**
 * scp_interface__emit_ready:
 * @object: A #scpinterface.
 *
 * Emits the <link linkend="gdbus-signal-org-fedoraproject-Config-Printing.ready">"ready"</link> D-Bus signal.
 */
void
scp_interface__emit_ready (
    scpinterface *object)
{
  g_signal_emit_by_name (object, "ready");
}

/**
 * scp_interface__emit_error:
 * @object: A #scpinterface.
 *
 * Emits the <link linkend="gdbus-signal-org-fedoraproject-Config-Printing.error">"error"</link> D-Bus signal.
 */
void
scp_interface__emit_error (
    scpinterface *object)
{
  g_signal_emit_by_name (object, "error");
}

/**
 * scp_interface__call_new_printer_dialog:
 * @proxy: A #scpinterfaceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.NewPrinterDialog">NewPrinterDialog()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface__call_new_printer_dialog_finish() to get the result of the operation.
 *
 * See scp_interface__call_new_printer_dialog_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface__call_new_printer_dialog (
    scpinterface *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NewPrinterDialog",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface__call_new_printer_dialog_finish:
 * @proxy: A #scpinterfaceProxy.
 * @out_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface__call_new_printer_dialog().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface__call_new_printer_dialog().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_new_printer_dialog_finish (
    scpinterface *proxy,
    gchar **out_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_new_printer_dialog_sync:
 * @proxy: A #scpinterfaceProxy.
 * @out_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.NewPrinterDialog">NewPrinterDialog()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface__call_new_printer_dialog() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_new_printer_dialog_sync (
    scpinterface *proxy,
    gchar **out_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NewPrinterDialog",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_printer_properties_dialog:
 * @proxy: A #scpinterfaceProxy.
 * @arg_xid: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.PrinterPropertiesDialog">PrinterPropertiesDialog()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface__call_printer_properties_dialog_finish() to get the result of the operation.
 *
 * See scp_interface__call_printer_properties_dialog_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface__call_printer_properties_dialog (
    scpinterface *proxy,
    guint arg_xid,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "PrinterPropertiesDialog",
    g_variant_new ("(us)",
                   arg_xid,
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface__call_printer_properties_dialog_finish:
 * @proxy: A #scpinterfaceProxy.
 * @out_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface__call_printer_properties_dialog().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface__call_printer_properties_dialog().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_printer_properties_dialog_finish (
    scpinterface *proxy,
    gchar **out_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_printer_properties_dialog_sync:
 * @proxy: A #scpinterfaceProxy.
 * @arg_xid: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @out_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.PrinterPropertiesDialog">PrinterPropertiesDialog()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface__call_printer_properties_dialog() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_printer_properties_dialog_sync (
    scpinterface *proxy,
    guint arg_xid,
    const gchar *arg_name,
    gchar **out_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "PrinterPropertiesDialog",
    g_variant_new ("(us)",
                   arg_xid,
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_job_applet:
 * @proxy: A #scpinterfaceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.JobApplet">JobApplet()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface__call_job_applet_finish() to get the result of the operation.
 *
 * See scp_interface__call_job_applet_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface__call_job_applet (
    scpinterface *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "JobApplet",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface__call_job_applet_finish:
 * @proxy: A #scpinterfaceProxy.
 * @out_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface__call_job_applet().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface__call_job_applet().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_job_applet_finish (
    scpinterface *proxy,
    gchar **out_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_job_applet_sync:
 * @proxy: A #scpinterfaceProxy.
 * @out_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.JobApplet">JobApplet()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface__call_job_applet() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_job_applet_sync (
    scpinterface *proxy,
    gchar **out_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "JobApplet",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_get_best_drivers:
 * @proxy: A #scpinterfaceProxy.
 * @arg_device_id: Argument to pass with the method invocation.
 * @arg_device_make_and_model: Argument to pass with the method invocation.
 * @arg_device_uri: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.GetBestDrivers">GetBestDrivers()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface__call_get_best_drivers_finish() to get the result of the operation.
 *
 * See scp_interface__call_get_best_drivers_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface__call_get_best_drivers (
    scpinterface *proxy,
    const gchar *arg_device_id,
    const gchar *arg_device_make_and_model,
    const gchar *arg_device_uri,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetBestDrivers",
    g_variant_new ("(sss)",
                   arg_device_id,
                   arg_device_make_and_model,
                   arg_device_uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface__call_get_best_drivers_finish:
 * @proxy: A #scpinterfaceProxy.
 * @out_drivers: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface__call_get_best_drivers().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface__call_get_best_drivers().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_get_best_drivers_finish (
    scpinterface *proxy,
    GVariant **out_drivers,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ss))",
                 out_drivers);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_get_best_drivers_sync:
 * @proxy: A #scpinterfaceProxy.
 * @arg_device_id: Argument to pass with the method invocation.
 * @arg_device_make_and_model: Argument to pass with the method invocation.
 * @arg_device_uri: Argument to pass with the method invocation.
 * @out_drivers: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.GetBestDrivers">GetBestDrivers()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface__call_get_best_drivers() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_get_best_drivers_sync (
    scpinterface *proxy,
    const gchar *arg_device_id,
    const gchar *arg_device_make_and_model,
    const gchar *arg_device_uri,
    GVariant **out_drivers,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetBestDrivers",
    g_variant_new ("(sss)",
                   arg_device_id,
                   arg_device_make_and_model,
                   arg_device_uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ss))",
                 out_drivers);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_missing_executables:
 * @proxy: A #scpinterfaceProxy.
 * @arg_ppd_filename: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.MissingExecutables">MissingExecutables()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface__call_missing_executables_finish() to get the result of the operation.
 *
 * See scp_interface__call_missing_executables_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface__call_missing_executables (
    scpinterface *proxy,
    const gchar *arg_ppd_filename,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "MissingExecutables",
    g_variant_new ("(s)",
                   arg_ppd_filename),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface__call_missing_executables_finish:
 * @proxy: A #scpinterfaceProxy.
 * @out_missing_executables: (out) (array zero-terminated=1): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface__call_missing_executables().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface__call_missing_executables().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_missing_executables_finish (
    scpinterface *proxy,
    gchar ***out_missing_executables,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^as)",
                 out_missing_executables);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_missing_executables_sync:
 * @proxy: A #scpinterfaceProxy.
 * @arg_ppd_filename: Argument to pass with the method invocation.
 * @out_missing_executables: (out) (array zero-terminated=1): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.MissingExecutables">MissingExecutables()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface__call_missing_executables() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_missing_executables_sync (
    scpinterface *proxy,
    const gchar *arg_ppd_filename,
    gchar ***out_missing_executables,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "MissingExecutables",
    g_variant_new ("(s)",
                   arg_ppd_filename),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^as)",
                 out_missing_executables);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_group_physical_devices:
 * @proxy: A #scpinterfaceProxy.
 * @arg_devices: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.GroupPhysicalDevices">GroupPhysicalDevices()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface__call_group_physical_devices_finish() to get the result of the operation.
 *
 * See scp_interface__call_group_physical_devices_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface__call_group_physical_devices (
    scpinterface *proxy,
    GVariant *arg_devices,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GroupPhysicalDevices",
    g_variant_new ("(@a{sa{ss}})",
                   arg_devices),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface__call_group_physical_devices_finish:
 * @proxy: A #scpinterfaceProxy.
 * @out_grouped_devices: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface__call_group_physical_devices().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface__call_group_physical_devices().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_group_physical_devices_finish (
    scpinterface *proxy,
    GVariant **out_grouped_devices,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@aas)",
                 out_grouped_devices);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__call_group_physical_devices_sync:
 * @proxy: A #scpinterfaceProxy.
 * @arg_devices: Argument to pass with the method invocation.
 * @out_grouped_devices: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.GroupPhysicalDevices">GroupPhysicalDevices()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface__call_group_physical_devices() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface__call_group_physical_devices_sync (
    scpinterface *proxy,
    GVariant *arg_devices,
    GVariant **out_grouped_devices,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GroupPhysicalDevices",
    g_variant_new ("(@a{sa{ss}})",
                   arg_devices),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@aas)",
                 out_grouped_devices);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface__complete_new_printer_dialog:
 * @object: A #scpinterface.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.NewPrinterDialog">NewPrinterDialog()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface__complete_new_printer_dialog (
    scpinterface *object,
    GDBusMethodInvocation *invocation,
    const gchar *path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   path));
}

/**
 * scp_interface__complete_printer_properties_dialog:
 * @object: A #scpinterface.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.PrinterPropertiesDialog">PrinterPropertiesDialog()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface__complete_printer_properties_dialog (
    scpinterface *object,
    GDBusMethodInvocation *invocation,
    const gchar *path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   path));
}

/**
 * scp_interface__complete_job_applet:
 * @object: A #scpinterface.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.JobApplet">JobApplet()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface__complete_job_applet (
    scpinterface *object,
    GDBusMethodInvocation *invocation,
    const gchar *path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   path));
}

/**
 * scp_interface__complete_get_best_drivers:
 * @object: A #scpinterface.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @drivers: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.GetBestDrivers">GetBestDrivers()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface__complete_get_best_drivers (
    scpinterface *object,
    GDBusMethodInvocation *invocation,
    GVariant *drivers)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(ss))",
                   drivers));
}

/**
 * scp_interface__complete_missing_executables:
 * @object: A #scpinterface.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @missing_executables: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.MissingExecutables">MissingExecutables()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface__complete_missing_executables (
    scpinterface *object,
    GDBusMethodInvocation *invocation,
    const gchar *const *missing_executables)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(^as)",
                   missing_executables));
}

/**
 * scp_interface__complete_group_physical_devices:
 * @object: A #scpinterface.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @grouped_devices: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing.GroupPhysicalDevices">GroupPhysicalDevices()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface__complete_group_physical_devices (
    scpinterface *object,
    GDBusMethodInvocation *invocation,
    GVariant *grouped_devices)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@aas)",
                   grouped_devices));
}

/* ------------------------------------------------------------------------ */

/**
 * scpinterfaceProxy:
 *
 * The #scpinterfaceProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * scpinterfaceProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #scpinterfaceProxy.
 */

struct _scpinterfaceProxyPrivate
{
  GData *qdata;
};

static void scp_interface__proxy_iface_init (scpinterfaceIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (scpinterfaceProxy, scp_interface__proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (scpinterfaceProxy)
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_, scp_interface__proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (scpinterfaceProxy, scp_interface__proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_, scp_interface__proxy_iface_init))

#endif
static void
scp_interface__proxy_finalize (GObject *object)
{
  scpinterfaceProxy *proxy = SCP_INTERFACE__PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (scp_interface__proxy_parent_class)->finalize (object);
}

static void
scp_interface__proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
scp_interface__proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
scp_interface__proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_scp_interface__interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], SCP_INTERFACE_TYPE_);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, SCP_INTERFACE_TYPE_);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
scp_interface__proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  scpinterfaceProxy *proxy = SCP_INTERFACE__PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface__interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface__interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
scp_interface__proxy_init (scpinterfaceProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = scp_interface__proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, SCP_INTERFACE_TYPE__PROXY, scpinterfaceProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), scp_interface__interface_info ());
}

static void
scp_interface__proxy_class_init (scpinterfaceProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = scp_interface__proxy_finalize;
  gobject_class->get_property = scp_interface__proxy_get_property;
  gobject_class->set_property = scp_interface__proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = scp_interface__proxy_g_signal;
  proxy_class->g_properties_changed = scp_interface__proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (scpinterfaceProxyPrivate));
#endif
}

static void
scp_interface__proxy_iface_init (scpinterfaceIface *iface)
{
}

/**
 * scp_interface__proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing.top_of_page">org.fedoraproject.Config.Printing</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface__proxy_new_finish() to get the result of the operation.
 *
 * See scp_interface__proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
scp_interface__proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (SCP_INTERFACE_TYPE__PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing", NULL);
}

/**
 * scp_interface__proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface__proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with scp_interface__proxy_new().
 *
 * Returns: (transfer full) (type scpinterfaceProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterface *
scp_interface__proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SCP_INTERFACE_ (ret);
  else
    return NULL;
}

/**
 * scp_interface__proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing.top_of_page">org.fedoraproject.Config.Printing</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See scp_interface__proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type scpinterfaceProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterface *
scp_interface__proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (SCP_INTERFACE_TYPE__PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing", NULL);
  if (ret != NULL)
    return SCP_INTERFACE_ (ret);
  else
    return NULL;
}


/**
 * scp_interface__proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like scp_interface__proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface__proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See scp_interface__proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
scp_interface__proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (SCP_INTERFACE_TYPE__PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing", NULL);
}

/**
 * scp_interface__proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface__proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with scp_interface__proxy_new_for_bus().
 *
 * Returns: (transfer full) (type scpinterfaceProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterface *
scp_interface__proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SCP_INTERFACE_ (ret);
  else
    return NULL;
}

/**
 * scp_interface__proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like scp_interface__proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See scp_interface__proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type scpinterfaceProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterface *
scp_interface__proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (SCP_INTERFACE_TYPE__PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing", NULL);
  if (ret != NULL)
    return SCP_INTERFACE_ (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * scpinterfaceSkeleton:
 *
 * The #scpinterfaceSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * scpinterfaceSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #scpinterfaceSkeleton.
 */

struct _scpinterfaceSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_scp_interface__skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  scpinterfaceSkeleton *skeleton = SCP_INTERFACE__SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], SCP_INTERFACE_TYPE_);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, SCP_INTERFACE_TYPE_);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_scp_interface__skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  scpinterfaceSkeleton *skeleton = SCP_INTERFACE__SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface__interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_scp_interface__skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  scpinterfaceSkeleton *skeleton = SCP_INTERFACE__SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface__interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _scp_interface__skeleton_vtable =
{
  _scp_interface__skeleton_handle_method_call,
  _scp_interface__skeleton_handle_get_property,
  _scp_interface__skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
scp_interface__skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return scp_interface__interface_info ();
}

static GDBusInterfaceVTable *
scp_interface__skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_scp_interface__skeleton_vtable;
}

static GVariant *
scp_interface__skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  scpinterfaceSkeleton *skeleton = SCP_INTERFACE__SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_scp_interface__interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _scp_interface__interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _scp_interface__interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _scp_interface__skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
scp_interface__skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_scp_interface__on_signal_ready (
    scpinterface *object)
{
  scpinterfaceSkeleton *skeleton = SCP_INTERFACE__SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing", "ready",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_scp_interface__on_signal_error (
    scpinterface *object)
{
  scpinterfaceSkeleton *skeleton = SCP_INTERFACE__SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing", "error",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void scp_interface__skeleton_iface_init (scpinterfaceIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (scpinterfaceSkeleton, scp_interface__skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (scpinterfaceSkeleton)
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_, scp_interface__skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (scpinterfaceSkeleton, scp_interface__skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_, scp_interface__skeleton_iface_init))

#endif
static void
scp_interface__skeleton_finalize (GObject *object)
{
  scpinterfaceSkeleton *skeleton = SCP_INTERFACE__SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (scp_interface__skeleton_parent_class)->finalize (object);
}

static void
scp_interface__skeleton_init (scpinterfaceSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = scp_interface__skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, SCP_INTERFACE_TYPE__SKELETON, scpinterfaceSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
scp_interface__skeleton_class_init (scpinterfaceSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = scp_interface__skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = scp_interface__skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = scp_interface__skeleton_dbus_interface_get_properties;
  skeleton_class->flush = scp_interface__skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = scp_interface__skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (scpinterfaceSkeletonPrivate));
#endif
}

static void
scp_interface__skeleton_iface_init (scpinterfaceIface *iface)
{
  iface->ready = _scp_interface__on_signal_ready;
  iface->error = _scp_interface__on_signal_error;
}

/**
 * scp_interface__skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing.top_of_page">org.fedoraproject.Config.Printing</link>.
 *
 * Returns: (transfer full) (type scpinterfaceSkeleton): The skeleton object.
 */
scpinterface *
scp_interface__skeleton_new (void)
{
  return SCP_INTERFACE_ (g_object_new (SCP_INTERFACE_TYPE__SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.fedoraproject.Config.Printing.NewPrinterDialog
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:scpinterfaceNewPrinterDialog
 * @title: scpinterfaceNewPrinterDialog
 * @short_description: Generated C code for the org.fedoraproject.Config.Printing.NewPrinterDialog D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-NewPrinterDialog.top_of_page">org.fedoraproject.Config.Printing.NewPrinterDialog</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.fedoraproject.Config.Printing.NewPrinterDialog ---- */

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_method_info_new_printer_from_device_IN_ARG_xid =
{
  {
    -1,
    (gchar *) "xid",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_method_info_new_printer_from_device_IN_ARG_device_uri =
{
  {
    -1,
    (gchar *) "device_uri",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_method_info_new_printer_from_device_IN_ARG_device_id =
{
  {
    -1,
    (gchar *) "device_id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface_new_printer_dialog_method_info_new_printer_from_device_IN_ARG_pointers[] =
{
  &_scp_interface_new_printer_dialog_method_info_new_printer_from_device_IN_ARG_xid,
  &_scp_interface_new_printer_dialog_method_info_new_printer_from_device_IN_ARG_device_uri,
  &_scp_interface_new_printer_dialog_method_info_new_printer_from_device_IN_ARG_device_id,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface_new_printer_dialog_method_info_new_printer_from_device =
{
  {
    -1,
    (gchar *) "NewPrinterFromDevice",
    (GDBusArgInfo **) &_scp_interface_new_printer_dialog_method_info_new_printer_from_device_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-new-printer-from-device",
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_method_info_download_driver_for_device_id_IN_ARG_xid =
{
  {
    -1,
    (gchar *) "xid",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_method_info_download_driver_for_device_id_IN_ARG_device_id =
{
  {
    -1,
    (gchar *) "device_id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface_new_printer_dialog_method_info_download_driver_for_device_id_IN_ARG_pointers[] =
{
  &_scp_interface_new_printer_dialog_method_info_download_driver_for_device_id_IN_ARG_xid,
  &_scp_interface_new_printer_dialog_method_info_download_driver_for_device_id_IN_ARG_device_id,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface_new_printer_dialog_method_info_download_driver_for_device_id =
{
  {
    -1,
    (gchar *) "DownloadDriverForDeviceID",
    (GDBusArgInfo **) &_scp_interface_new_printer_dialog_method_info_download_driver_for_device_id_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-download-driver-for-device-id",
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_method_info_change_ppd_IN_ARG_xid =
{
  {
    -1,
    (gchar *) "xid",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_method_info_change_ppd_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_method_info_change_ppd_IN_ARG_device_id =
{
  {
    -1,
    (gchar *) "device_id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface_new_printer_dialog_method_info_change_ppd_IN_ARG_pointers[] =
{
  &_scp_interface_new_printer_dialog_method_info_change_ppd_IN_ARG_xid,
  &_scp_interface_new_printer_dialog_method_info_change_ppd_IN_ARG_name,
  &_scp_interface_new_printer_dialog_method_info_change_ppd_IN_ARG_device_id,
  NULL
};

static const _ExtendedGDBusMethodInfo _scp_interface_new_printer_dialog_method_info_change_ppd =
{
  {
    -1,
    (gchar *) "ChangePPD",
    (GDBusArgInfo **) &_scp_interface_new_printer_dialog_method_info_change_ppd_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-change-ppd",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _scp_interface_new_printer_dialog_method_info_pointers[] =
{
  &_scp_interface_new_printer_dialog_method_info_new_printer_from_device,
  &_scp_interface_new_printer_dialog_method_info_download_driver_for_device_id,
  &_scp_interface_new_printer_dialog_method_info_change_ppd,
  NULL
};

static const _ExtendedGDBusSignalInfo _scp_interface_new_printer_dialog_signal_info_dialog_canceled =
{
  {
    -1,
    (gchar *) "DialogCanceled",
    NULL,
    NULL
  },
  "dialog-canceled"
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_signal_info_printer_added_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface_new_printer_dialog_signal_info_printer_added_ARG_pointers[] =
{
  &_scp_interface_new_printer_dialog_signal_info_printer_added_ARG_name,
  NULL
};

static const _ExtendedGDBusSignalInfo _scp_interface_new_printer_dialog_signal_info_printer_added =
{
  {
    -1,
    (gchar *) "PrinterAdded",
    (GDBusArgInfo **) &_scp_interface_new_printer_dialog_signal_info_printer_added_ARG_pointers,
    NULL
  },
  "printer-added"
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_signal_info_printer_modified_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_signal_info_printer_modified_ARG_ppd_has_changed =
{
  {
    -1,
    (gchar *) "ppd_has_changed",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface_new_printer_dialog_signal_info_printer_modified_ARG_pointers[] =
{
  &_scp_interface_new_printer_dialog_signal_info_printer_modified_ARG_name,
  &_scp_interface_new_printer_dialog_signal_info_printer_modified_ARG_ppd_has_changed,
  NULL
};

static const _ExtendedGDBusSignalInfo _scp_interface_new_printer_dialog_signal_info_printer_modified =
{
  {
    -1,
    (gchar *) "PrinterModified",
    (GDBusArgInfo **) &_scp_interface_new_printer_dialog_signal_info_printer_modified_ARG_pointers,
    NULL
  },
  "printer-modified"
};

static const _ExtendedGDBusArgInfo _scp_interface_new_printer_dialog_signal_info_driver_download_checked_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _scp_interface_new_printer_dialog_signal_info_driver_download_checked_ARG_pointers[] =
{
  &_scp_interface_new_printer_dialog_signal_info_driver_download_checked_ARG_name,
  NULL
};

static const _ExtendedGDBusSignalInfo _scp_interface_new_printer_dialog_signal_info_driver_download_checked =
{
  {
    -1,
    (gchar *) "DriverDownloadChecked",
    (GDBusArgInfo **) &_scp_interface_new_printer_dialog_signal_info_driver_download_checked_ARG_pointers,
    NULL
  },
  "driver-download-checked"
};

static const _ExtendedGDBusSignalInfo * const _scp_interface_new_printer_dialog_signal_info_pointers[] =
{
  &_scp_interface_new_printer_dialog_signal_info_dialog_canceled,
  &_scp_interface_new_printer_dialog_signal_info_printer_added,
  &_scp_interface_new_printer_dialog_signal_info_printer_modified,
  &_scp_interface_new_printer_dialog_signal_info_driver_download_checked,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _scp_interface_new_printer_dialog_interface_info =
{
  {
    -1,
    (gchar *) "org.fedoraproject.Config.Printing.NewPrinterDialog",
    (GDBusMethodInfo **) &_scp_interface_new_printer_dialog_method_info_pointers,
    (GDBusSignalInfo **) &_scp_interface_new_printer_dialog_signal_info_pointers,
    NULL,
    NULL
  },
  "new-printer-dialog",
};


/**
 * scp_interface_new_printer_dialog_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-NewPrinterDialog.top_of_page">org.fedoraproject.Config.Printing.NewPrinterDialog</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
scp_interface_new_printer_dialog_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_scp_interface_new_printer_dialog_interface_info.parent_struct;
}

/**
 * scp_interface_new_printer_dialog_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #scpinterfaceNewPrinterDialog interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
scp_interface_new_printer_dialog_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * scpinterfaceNewPrinterDialog:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-NewPrinterDialog.top_of_page">org.fedoraproject.Config.Printing.NewPrinterDialog</link>.
 */

/**
 * scpinterfaceNewPrinterDialogIface:
 * @parent_iface: The parent interface.
 * @handle_change_ppd: Handler for the #scpinterfaceNewPrinterDialog::handle-change-ppd signal.
 * @handle_download_driver_for_device_id: Handler for the #scpinterfaceNewPrinterDialog::handle-download-driver-for-device-id signal.
 * @handle_new_printer_from_device: Handler for the #scpinterfaceNewPrinterDialog::handle-new-printer-from-device signal.
 * @dialog_canceled: Handler for the #scpinterfaceNewPrinterDialog::dialog-canceled signal.
 * @driver_download_checked: Handler for the #scpinterfaceNewPrinterDialog::driver-download-checked signal.
 * @printer_added: Handler for the #scpinterfaceNewPrinterDialog::printer-added signal.
 * @printer_modified: Handler for the #scpinterfaceNewPrinterDialog::printer-modified signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-NewPrinterDialog.top_of_page">org.fedoraproject.Config.Printing.NewPrinterDialog</link>.
 */

typedef scpinterfaceNewPrinterDialogIface scpinterfaceNewPrinterDialogInterface;
G_DEFINE_INTERFACE (scpinterfaceNewPrinterDialog, scp_interface_new_printer_dialog, G_TYPE_OBJECT)

static void
scp_interface_new_printer_dialog_default_init (scpinterfaceNewPrinterDialogIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * scpinterfaceNewPrinterDialog::handle-new-printer-from-device:
   * @object: A #scpinterfaceNewPrinterDialog.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_xid: Argument passed by remote caller.
   * @arg_device_uri: Argument passed by remote caller.
   * @arg_device_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.NewPrinterFromDevice">NewPrinterFromDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface_new_printer_dialog_complete_new_printer_from_device() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-new-printer-from-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceNewPrinterDialogIface, handle_new_printer_from_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * scpinterfaceNewPrinterDialog::handle-download-driver-for-device-id:
   * @object: A #scpinterfaceNewPrinterDialog.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_xid: Argument passed by remote caller.
   * @arg_device_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.DownloadDriverForDeviceID">DownloadDriverForDeviceID()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface_new_printer_dialog_complete_download_driver_for_device_id() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-download-driver-for-device-id",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceNewPrinterDialogIface, handle_download_driver_for_device_id),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_STRING);

  /**
   * scpinterfaceNewPrinterDialog::handle-change-ppd:
   * @object: A #scpinterfaceNewPrinterDialog.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_xid: Argument passed by remote caller.
   * @arg_name: Argument passed by remote caller.
   * @arg_device_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.ChangePPD">ChangePPD()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface_new_printer_dialog_complete_change_ppd() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-change-ppd",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceNewPrinterDialogIface, handle_change_ppd),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_STRING, G_TYPE_STRING);

  /* GObject signals for received D-Bus signals: */
  /**
   * scpinterfaceNewPrinterDialog::dialog-canceled:
   * @object: A #scpinterfaceNewPrinterDialog.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-NewPrinterDialog.DialogCanceled">"DialogCanceled"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("dialog-canceled",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceNewPrinterDialogIface, dialog_canceled),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

  /**
   * scpinterfaceNewPrinterDialog::printer-added:
   * @object: A #scpinterfaceNewPrinterDialog.
   * @arg_name: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-NewPrinterDialog.PrinterAdded">"PrinterAdded"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("printer-added",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceNewPrinterDialogIface, printer_added),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

  /**
   * scpinterfaceNewPrinterDialog::printer-modified:
   * @object: A #scpinterfaceNewPrinterDialog.
   * @arg_name: Argument.
   * @arg_ppd_has_changed: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-NewPrinterDialog.PrinterModified">"PrinterModified"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("printer-modified",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceNewPrinterDialogIface, printer_modified),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_BOOLEAN);

  /**
   * scpinterfaceNewPrinterDialog::driver-download-checked:
   * @object: A #scpinterfaceNewPrinterDialog.
   * @arg_name: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-NewPrinterDialog.DriverDownloadChecked">"DriverDownloadChecked"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("driver-download-checked",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceNewPrinterDialogIface, driver_download_checked),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

}

/**
 * scp_interface_new_printer_dialog_emit_dialog_canceled:
 * @object: A #scpinterfaceNewPrinterDialog.
 *
 * Emits the <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-NewPrinterDialog.DialogCanceled">"DialogCanceled"</link> D-Bus signal.
 */
void
scp_interface_new_printer_dialog_emit_dialog_canceled (
    scpinterfaceNewPrinterDialog *object)
{
  g_signal_emit_by_name (object, "dialog-canceled");
}

/**
 * scp_interface_new_printer_dialog_emit_printer_added:
 * @object: A #scpinterfaceNewPrinterDialog.
 * @arg_name: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-NewPrinterDialog.PrinterAdded">"PrinterAdded"</link> D-Bus signal.
 */
void
scp_interface_new_printer_dialog_emit_printer_added (
    scpinterfaceNewPrinterDialog *object,
    const gchar *arg_name)
{
  g_signal_emit_by_name (object, "printer-added", arg_name);
}

/**
 * scp_interface_new_printer_dialog_emit_printer_modified:
 * @object: A #scpinterfaceNewPrinterDialog.
 * @arg_name: Argument to pass with the signal.
 * @arg_ppd_has_changed: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-NewPrinterDialog.PrinterModified">"PrinterModified"</link> D-Bus signal.
 */
void
scp_interface_new_printer_dialog_emit_printer_modified (
    scpinterfaceNewPrinterDialog *object,
    const gchar *arg_name,
    gboolean arg_ppd_has_changed)
{
  g_signal_emit_by_name (object, "printer-modified", arg_name, arg_ppd_has_changed);
}

/**
 * scp_interface_new_printer_dialog_emit_driver_download_checked:
 * @object: A #scpinterfaceNewPrinterDialog.
 * @arg_name: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-NewPrinterDialog.DriverDownloadChecked">"DriverDownloadChecked"</link> D-Bus signal.
 */
void
scp_interface_new_printer_dialog_emit_driver_download_checked (
    scpinterfaceNewPrinterDialog *object,
    const gchar *arg_name)
{
  g_signal_emit_by_name (object, "driver-download-checked", arg_name);
}

/**
 * scp_interface_new_printer_dialog_call_new_printer_from_device:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @arg_xid: Argument to pass with the method invocation.
 * @arg_device_uri: Argument to pass with the method invocation.
 * @arg_device_id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.NewPrinterFromDevice">NewPrinterFromDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_new_printer_dialog_call_new_printer_from_device_finish() to get the result of the operation.
 *
 * See scp_interface_new_printer_dialog_call_new_printer_from_device_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface_new_printer_dialog_call_new_printer_from_device (
    scpinterfaceNewPrinterDialog *proxy,
    guint arg_xid,
    const gchar *arg_device_uri,
    const gchar *arg_device_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NewPrinterFromDevice",
    g_variant_new ("(uss)",
                   arg_xid,
                   arg_device_uri,
                   arg_device_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface_new_printer_dialog_call_new_printer_from_device_finish:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_new_printer_dialog_call_new_printer_from_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface_new_printer_dialog_call_new_printer_from_device().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_new_printer_dialog_call_new_printer_from_device_finish (
    scpinterfaceNewPrinterDialog *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_new_printer_dialog_call_new_printer_from_device_sync:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @arg_xid: Argument to pass with the method invocation.
 * @arg_device_uri: Argument to pass with the method invocation.
 * @arg_device_id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.NewPrinterFromDevice">NewPrinterFromDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface_new_printer_dialog_call_new_printer_from_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_new_printer_dialog_call_new_printer_from_device_sync (
    scpinterfaceNewPrinterDialog *proxy,
    guint arg_xid,
    const gchar *arg_device_uri,
    const gchar *arg_device_id,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NewPrinterFromDevice",
    g_variant_new ("(uss)",
                   arg_xid,
                   arg_device_uri,
                   arg_device_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_new_printer_dialog_call_download_driver_for_device_id:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @arg_xid: Argument to pass with the method invocation.
 * @arg_device_id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.DownloadDriverForDeviceID">DownloadDriverForDeviceID()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_new_printer_dialog_call_download_driver_for_device_id_finish() to get the result of the operation.
 *
 * See scp_interface_new_printer_dialog_call_download_driver_for_device_id_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface_new_printer_dialog_call_download_driver_for_device_id (
    scpinterfaceNewPrinterDialog *proxy,
    guint arg_xid,
    const gchar *arg_device_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "DownloadDriverForDeviceID",
    g_variant_new ("(us)",
                   arg_xid,
                   arg_device_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface_new_printer_dialog_call_download_driver_for_device_id_finish:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_new_printer_dialog_call_download_driver_for_device_id().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface_new_printer_dialog_call_download_driver_for_device_id().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_new_printer_dialog_call_download_driver_for_device_id_finish (
    scpinterfaceNewPrinterDialog *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_new_printer_dialog_call_download_driver_for_device_id_sync:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @arg_xid: Argument to pass with the method invocation.
 * @arg_device_id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.DownloadDriverForDeviceID">DownloadDriverForDeviceID()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface_new_printer_dialog_call_download_driver_for_device_id() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_new_printer_dialog_call_download_driver_for_device_id_sync (
    scpinterfaceNewPrinterDialog *proxy,
    guint arg_xid,
    const gchar *arg_device_id,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "DownloadDriverForDeviceID",
    g_variant_new ("(us)",
                   arg_xid,
                   arg_device_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_new_printer_dialog_call_change_ppd:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @arg_xid: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_device_id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.ChangePPD">ChangePPD()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_new_printer_dialog_call_change_ppd_finish() to get the result of the operation.
 *
 * See scp_interface_new_printer_dialog_call_change_ppd_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface_new_printer_dialog_call_change_ppd (
    scpinterfaceNewPrinterDialog *proxy,
    guint arg_xid,
    const gchar *arg_name,
    const gchar *arg_device_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ChangePPD",
    g_variant_new ("(uss)",
                   arg_xid,
                   arg_name,
                   arg_device_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface_new_printer_dialog_call_change_ppd_finish:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_new_printer_dialog_call_change_ppd().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface_new_printer_dialog_call_change_ppd().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_new_printer_dialog_call_change_ppd_finish (
    scpinterfaceNewPrinterDialog *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_new_printer_dialog_call_change_ppd_sync:
 * @proxy: A #scpinterfaceNewPrinterDialogProxy.
 * @arg_xid: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_device_id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.ChangePPD">ChangePPD()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface_new_printer_dialog_call_change_ppd() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_new_printer_dialog_call_change_ppd_sync (
    scpinterfaceNewPrinterDialog *proxy,
    guint arg_xid,
    const gchar *arg_name,
    const gchar *arg_device_id,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ChangePPD",
    g_variant_new ("(uss)",
                   arg_xid,
                   arg_name,
                   arg_device_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_new_printer_dialog_complete_new_printer_from_device:
 * @object: A #scpinterfaceNewPrinterDialog.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.NewPrinterFromDevice">NewPrinterFromDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface_new_printer_dialog_complete_new_printer_from_device (
    scpinterfaceNewPrinterDialog *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * scp_interface_new_printer_dialog_complete_download_driver_for_device_id:
 * @object: A #scpinterfaceNewPrinterDialog.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.DownloadDriverForDeviceID">DownloadDriverForDeviceID()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface_new_printer_dialog_complete_download_driver_for_device_id (
    scpinterfaceNewPrinterDialog *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * scp_interface_new_printer_dialog_complete_change_ppd:
 * @object: A #scpinterfaceNewPrinterDialog.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-NewPrinterDialog.ChangePPD">ChangePPD()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface_new_printer_dialog_complete_change_ppd (
    scpinterfaceNewPrinterDialog *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * scpinterfaceNewPrinterDialogProxy:
 *
 * The #scpinterfaceNewPrinterDialogProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * scpinterfaceNewPrinterDialogProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #scpinterfaceNewPrinterDialogProxy.
 */

struct _scpinterfaceNewPrinterDialogProxyPrivate
{
  GData *qdata;
};

static void scp_interface_new_printer_dialog_proxy_iface_init (scpinterfaceNewPrinterDialogIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (scpinterfaceNewPrinterDialogProxy, scp_interface_new_printer_dialog_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (scpinterfaceNewPrinterDialogProxy)
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG, scp_interface_new_printer_dialog_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (scpinterfaceNewPrinterDialogProxy, scp_interface_new_printer_dialog_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG, scp_interface_new_printer_dialog_proxy_iface_init))

#endif
static void
scp_interface_new_printer_dialog_proxy_finalize (GObject *object)
{
  scpinterfaceNewPrinterDialogProxy *proxy = SCP_INTERFACE_NEW_PRINTER_DIALOG_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (scp_interface_new_printer_dialog_proxy_parent_class)->finalize (object);
}

static void
scp_interface_new_printer_dialog_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
scp_interface_new_printer_dialog_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
scp_interface_new_printer_dialog_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_scp_interface_new_printer_dialog_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
scp_interface_new_printer_dialog_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  scpinterfaceNewPrinterDialogProxy *proxy = SCP_INTERFACE_NEW_PRINTER_DIALOG_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_new_printer_dialog_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_new_printer_dialog_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
scp_interface_new_printer_dialog_proxy_init (scpinterfaceNewPrinterDialogProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = scp_interface_new_printer_dialog_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG_PROXY, scpinterfaceNewPrinterDialogProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), scp_interface_new_printer_dialog_interface_info ());
}

static void
scp_interface_new_printer_dialog_proxy_class_init (scpinterfaceNewPrinterDialogProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = scp_interface_new_printer_dialog_proxy_finalize;
  gobject_class->get_property = scp_interface_new_printer_dialog_proxy_get_property;
  gobject_class->set_property = scp_interface_new_printer_dialog_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = scp_interface_new_printer_dialog_proxy_g_signal;
  proxy_class->g_properties_changed = scp_interface_new_printer_dialog_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (scpinterfaceNewPrinterDialogProxyPrivate));
#endif
}

static void
scp_interface_new_printer_dialog_proxy_iface_init (scpinterfaceNewPrinterDialogIface *iface)
{
}

/**
 * scp_interface_new_printer_dialog_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-NewPrinterDialog.top_of_page">org.fedoraproject.Config.Printing.NewPrinterDialog</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_new_printer_dialog_proxy_new_finish() to get the result of the operation.
 *
 * See scp_interface_new_printer_dialog_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
scp_interface_new_printer_dialog_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.NewPrinterDialog", NULL);
}

/**
 * scp_interface_new_printer_dialog_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_new_printer_dialog_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with scp_interface_new_printer_dialog_proxy_new().
 *
 * Returns: (transfer full) (type scpinterfaceNewPrinterDialogProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfaceNewPrinterDialog *
scp_interface_new_printer_dialog_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SCP_INTERFACE_NEW_PRINTER_DIALOG (ret);
  else
    return NULL;
}

/**
 * scp_interface_new_printer_dialog_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-NewPrinterDialog.top_of_page">org.fedoraproject.Config.Printing.NewPrinterDialog</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See scp_interface_new_printer_dialog_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type scpinterfaceNewPrinterDialogProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfaceNewPrinterDialog *
scp_interface_new_printer_dialog_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.NewPrinterDialog", NULL);
  if (ret != NULL)
    return SCP_INTERFACE_NEW_PRINTER_DIALOG (ret);
  else
    return NULL;
}


/**
 * scp_interface_new_printer_dialog_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like scp_interface_new_printer_dialog_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_new_printer_dialog_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See scp_interface_new_printer_dialog_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
scp_interface_new_printer_dialog_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.NewPrinterDialog", NULL);
}

/**
 * scp_interface_new_printer_dialog_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_new_printer_dialog_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with scp_interface_new_printer_dialog_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type scpinterfaceNewPrinterDialogProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfaceNewPrinterDialog *
scp_interface_new_printer_dialog_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SCP_INTERFACE_NEW_PRINTER_DIALOG (ret);
  else
    return NULL;
}

/**
 * scp_interface_new_printer_dialog_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like scp_interface_new_printer_dialog_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See scp_interface_new_printer_dialog_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type scpinterfaceNewPrinterDialogProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfaceNewPrinterDialog *
scp_interface_new_printer_dialog_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.NewPrinterDialog", NULL);
  if (ret != NULL)
    return SCP_INTERFACE_NEW_PRINTER_DIALOG (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * scpinterfaceNewPrinterDialogSkeleton:
 *
 * The #scpinterfaceNewPrinterDialogSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * scpinterfaceNewPrinterDialogSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #scpinterfaceNewPrinterDialogSkeleton.
 */

struct _scpinterfaceNewPrinterDialogSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_scp_interface_new_printer_dialog_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_scp_interface_new_printer_dialog_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_new_printer_dialog_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_scp_interface_new_printer_dialog_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_new_printer_dialog_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _scp_interface_new_printer_dialog_skeleton_vtable =
{
  _scp_interface_new_printer_dialog_skeleton_handle_method_call,
  _scp_interface_new_printer_dialog_skeleton_handle_get_property,
  _scp_interface_new_printer_dialog_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
scp_interface_new_printer_dialog_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return scp_interface_new_printer_dialog_interface_info ();
}

static GDBusInterfaceVTable *
scp_interface_new_printer_dialog_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_scp_interface_new_printer_dialog_skeleton_vtable;
}

static GVariant *
scp_interface_new_printer_dialog_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_scp_interface_new_printer_dialog_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _scp_interface_new_printer_dialog_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _scp_interface_new_printer_dialog_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _scp_interface_new_printer_dialog_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.NewPrinterDialog", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
scp_interface_new_printer_dialog_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_scp_interface_new_printer_dialog_on_signal_dialog_canceled (
    scpinterfaceNewPrinterDialog *object)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.NewPrinterDialog", "DialogCanceled",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_scp_interface_new_printer_dialog_on_signal_printer_added (
    scpinterfaceNewPrinterDialog *object,
    const gchar *arg_name)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s)",
                   arg_name));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.NewPrinterDialog", "PrinterAdded",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_scp_interface_new_printer_dialog_on_signal_printer_modified (
    scpinterfaceNewPrinterDialog *object,
    const gchar *arg_name,
    gboolean arg_ppd_has_changed)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(sb)",
                   arg_name,
                   arg_ppd_has_changed));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.NewPrinterDialog", "PrinterModified",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_scp_interface_new_printer_dialog_on_signal_driver_download_checked (
    scpinterfaceNewPrinterDialog *object,
    const gchar *arg_name)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s)",
                   arg_name));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.NewPrinterDialog", "DriverDownloadChecked",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void scp_interface_new_printer_dialog_skeleton_iface_init (scpinterfaceNewPrinterDialogIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (scpinterfaceNewPrinterDialogSkeleton, scp_interface_new_printer_dialog_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (scpinterfaceNewPrinterDialogSkeleton)
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG, scp_interface_new_printer_dialog_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (scpinterfaceNewPrinterDialogSkeleton, scp_interface_new_printer_dialog_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG, scp_interface_new_printer_dialog_skeleton_iface_init))

#endif
static void
scp_interface_new_printer_dialog_skeleton_finalize (GObject *object)
{
  scpinterfaceNewPrinterDialogSkeleton *skeleton = SCP_INTERFACE_NEW_PRINTER_DIALOG_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (scp_interface_new_printer_dialog_skeleton_parent_class)->finalize (object);
}

static void
scp_interface_new_printer_dialog_skeleton_init (scpinterfaceNewPrinterDialogSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = scp_interface_new_printer_dialog_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG_SKELETON, scpinterfaceNewPrinterDialogSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
scp_interface_new_printer_dialog_skeleton_class_init (scpinterfaceNewPrinterDialogSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = scp_interface_new_printer_dialog_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = scp_interface_new_printer_dialog_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = scp_interface_new_printer_dialog_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = scp_interface_new_printer_dialog_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = scp_interface_new_printer_dialog_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (scpinterfaceNewPrinterDialogSkeletonPrivate));
#endif
}

static void
scp_interface_new_printer_dialog_skeleton_iface_init (scpinterfaceNewPrinterDialogIface *iface)
{
  iface->dialog_canceled = _scp_interface_new_printer_dialog_on_signal_dialog_canceled;
  iface->printer_added = _scp_interface_new_printer_dialog_on_signal_printer_added;
  iface->printer_modified = _scp_interface_new_printer_dialog_on_signal_printer_modified;
  iface->driver_download_checked = _scp_interface_new_printer_dialog_on_signal_driver_download_checked;
}

/**
 * scp_interface_new_printer_dialog_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-NewPrinterDialog.top_of_page">org.fedoraproject.Config.Printing.NewPrinterDialog</link>.
 *
 * Returns: (transfer full) (type scpinterfaceNewPrinterDialogSkeleton): The skeleton object.
 */
scpinterfaceNewPrinterDialog *
scp_interface_new_printer_dialog_skeleton_new (void)
{
  return SCP_INTERFACE_NEW_PRINTER_DIALOG (g_object_new (SCP_INTERFACE_TYPE_NEW_PRINTER_DIALOG_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.fedoraproject.Config.Printing.PrinterPropertiesDialog
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:scpinterfacePrinterPropertiesDialog
 * @title: scpinterfacePrinterPropertiesDialog
 * @short_description: Generated C code for the org.fedoraproject.Config.Printing.PrinterPropertiesDialog D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.top_of_page">org.fedoraproject.Config.Printing.PrinterPropertiesDialog</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.fedoraproject.Config.Printing.PrinterPropertiesDialog ---- */

static const _ExtendedGDBusMethodInfo _scp_interface_printer_properties_dialog_method_info_print_test_page =
{
  {
    -1,
    (gchar *) "PrintTestPage",
    NULL,
    NULL,
    NULL
  },
  "handle-print-test-page",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _scp_interface_printer_properties_dialog_method_info_pointers[] =
{
  &_scp_interface_printer_properties_dialog_method_info_print_test_page,
  NULL
};

static const _ExtendedGDBusSignalInfo _scp_interface_printer_properties_dialog_signal_info_finished =
{
  {
    -1,
    (gchar *) "Finished",
    NULL,
    NULL
  },
  "finished"
};

static const _ExtendedGDBusSignalInfo * const _scp_interface_printer_properties_dialog_signal_info_pointers[] =
{
  &_scp_interface_printer_properties_dialog_signal_info_finished,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _scp_interface_printer_properties_dialog_interface_info =
{
  {
    -1,
    (gchar *) "org.fedoraproject.Config.Printing.PrinterPropertiesDialog",
    (GDBusMethodInfo **) &_scp_interface_printer_properties_dialog_method_info_pointers,
    (GDBusSignalInfo **) &_scp_interface_printer_properties_dialog_signal_info_pointers,
    NULL,
    NULL
  },
  "printer-properties-dialog",
};


/**
 * scp_interface_printer_properties_dialog_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.top_of_page">org.fedoraproject.Config.Printing.PrinterPropertiesDialog</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
scp_interface_printer_properties_dialog_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_scp_interface_printer_properties_dialog_interface_info.parent_struct;
}

/**
 * scp_interface_printer_properties_dialog_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #scpinterfacePrinterPropertiesDialog interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
scp_interface_printer_properties_dialog_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * scpinterfacePrinterPropertiesDialog:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.top_of_page">org.fedoraproject.Config.Printing.PrinterPropertiesDialog</link>.
 */

/**
 * scpinterfacePrinterPropertiesDialogIface:
 * @parent_iface: The parent interface.
 * @handle_print_test_page: Handler for the #scpinterfacePrinterPropertiesDialog::handle-print-test-page signal.
 * @finished: Handler for the #scpinterfacePrinterPropertiesDialog::finished signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.top_of_page">org.fedoraproject.Config.Printing.PrinterPropertiesDialog</link>.
 */

typedef scpinterfacePrinterPropertiesDialogIface scpinterfacePrinterPropertiesDialogInterface;
G_DEFINE_INTERFACE (scpinterfacePrinterPropertiesDialog, scp_interface_printer_properties_dialog, G_TYPE_OBJECT)

static void
scp_interface_printer_properties_dialog_default_init (scpinterfacePrinterPropertiesDialogIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * scpinterfacePrinterPropertiesDialog::handle-print-test-page:
   * @object: A #scpinterfacePrinterPropertiesDialog.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.PrintTestPage">PrintTestPage()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface_printer_properties_dialog_complete_print_test_page() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-print-test-page",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfacePrinterPropertiesDialogIface, handle_print_test_page),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * scpinterfacePrinterPropertiesDialog::finished:
   * @object: A #scpinterfacePrinterPropertiesDialog.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.Finished">"Finished"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("finished",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfacePrinterPropertiesDialogIface, finished),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

}

/**
 * scp_interface_printer_properties_dialog_emit_finished:
 * @object: A #scpinterfacePrinterPropertiesDialog.
 *
 * Emits the <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.Finished">"Finished"</link> D-Bus signal.
 */
void
scp_interface_printer_properties_dialog_emit_finished (
    scpinterfacePrinterPropertiesDialog *object)
{
  g_signal_emit_by_name (object, "finished");
}

/**
 * scp_interface_printer_properties_dialog_call_print_test_page:
 * @proxy: A #scpinterfacePrinterPropertiesDialogProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.PrintTestPage">PrintTestPage()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_printer_properties_dialog_call_print_test_page_finish() to get the result of the operation.
 *
 * See scp_interface_printer_properties_dialog_call_print_test_page_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface_printer_properties_dialog_call_print_test_page (
    scpinterfacePrinterPropertiesDialog *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "PrintTestPage",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface_printer_properties_dialog_call_print_test_page_finish:
 * @proxy: A #scpinterfacePrinterPropertiesDialogProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_printer_properties_dialog_call_print_test_page().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface_printer_properties_dialog_call_print_test_page().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_printer_properties_dialog_call_print_test_page_finish (
    scpinterfacePrinterPropertiesDialog *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_printer_properties_dialog_call_print_test_page_sync:
 * @proxy: A #scpinterfacePrinterPropertiesDialogProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.PrintTestPage">PrintTestPage()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface_printer_properties_dialog_call_print_test_page() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_printer_properties_dialog_call_print_test_page_sync (
    scpinterfacePrinterPropertiesDialog *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "PrintTestPage",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_printer_properties_dialog_complete_print_test_page:
 * @object: A #scpinterfacePrinterPropertiesDialog.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.PrintTestPage">PrintTestPage()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface_printer_properties_dialog_complete_print_test_page (
    scpinterfacePrinterPropertiesDialog *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * scpinterfacePrinterPropertiesDialogProxy:
 *
 * The #scpinterfacePrinterPropertiesDialogProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * scpinterfacePrinterPropertiesDialogProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #scpinterfacePrinterPropertiesDialogProxy.
 */

struct _scpinterfacePrinterPropertiesDialogProxyPrivate
{
  GData *qdata;
};

static void scp_interface_printer_properties_dialog_proxy_iface_init (scpinterfacePrinterPropertiesDialogIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (scpinterfacePrinterPropertiesDialogProxy, scp_interface_printer_properties_dialog_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (scpinterfacePrinterPropertiesDialogProxy)
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG, scp_interface_printer_properties_dialog_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (scpinterfacePrinterPropertiesDialogProxy, scp_interface_printer_properties_dialog_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG, scp_interface_printer_properties_dialog_proxy_iface_init))

#endif
static void
scp_interface_printer_properties_dialog_proxy_finalize (GObject *object)
{
  scpinterfacePrinterPropertiesDialogProxy *proxy = SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (scp_interface_printer_properties_dialog_proxy_parent_class)->finalize (object);
}

static void
scp_interface_printer_properties_dialog_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
scp_interface_printer_properties_dialog_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
scp_interface_printer_properties_dialog_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_scp_interface_printer_properties_dialog_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
scp_interface_printer_properties_dialog_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  scpinterfacePrinterPropertiesDialogProxy *proxy = SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_printer_properties_dialog_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_printer_properties_dialog_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
scp_interface_printer_properties_dialog_proxy_init (scpinterfacePrinterPropertiesDialogProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = scp_interface_printer_properties_dialog_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG_PROXY, scpinterfacePrinterPropertiesDialogProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), scp_interface_printer_properties_dialog_interface_info ());
}

static void
scp_interface_printer_properties_dialog_proxy_class_init (scpinterfacePrinterPropertiesDialogProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = scp_interface_printer_properties_dialog_proxy_finalize;
  gobject_class->get_property = scp_interface_printer_properties_dialog_proxy_get_property;
  gobject_class->set_property = scp_interface_printer_properties_dialog_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = scp_interface_printer_properties_dialog_proxy_g_signal;
  proxy_class->g_properties_changed = scp_interface_printer_properties_dialog_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (scpinterfacePrinterPropertiesDialogProxyPrivate));
#endif
}

static void
scp_interface_printer_properties_dialog_proxy_iface_init (scpinterfacePrinterPropertiesDialogIface *iface)
{
}

/**
 * scp_interface_printer_properties_dialog_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.top_of_page">org.fedoraproject.Config.Printing.PrinterPropertiesDialog</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_printer_properties_dialog_proxy_new_finish() to get the result of the operation.
 *
 * See scp_interface_printer_properties_dialog_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
scp_interface_printer_properties_dialog_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.PrinterPropertiesDialog", NULL);
}

/**
 * scp_interface_printer_properties_dialog_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_printer_properties_dialog_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with scp_interface_printer_properties_dialog_proxy_new().
 *
 * Returns: (transfer full) (type scpinterfacePrinterPropertiesDialogProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfacePrinterPropertiesDialog *
scp_interface_printer_properties_dialog_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG (ret);
  else
    return NULL;
}

/**
 * scp_interface_printer_properties_dialog_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.top_of_page">org.fedoraproject.Config.Printing.PrinterPropertiesDialog</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See scp_interface_printer_properties_dialog_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type scpinterfacePrinterPropertiesDialogProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfacePrinterPropertiesDialog *
scp_interface_printer_properties_dialog_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.PrinterPropertiesDialog", NULL);
  if (ret != NULL)
    return SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG (ret);
  else
    return NULL;
}


/**
 * scp_interface_printer_properties_dialog_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like scp_interface_printer_properties_dialog_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_printer_properties_dialog_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See scp_interface_printer_properties_dialog_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
scp_interface_printer_properties_dialog_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.PrinterPropertiesDialog", NULL);
}

/**
 * scp_interface_printer_properties_dialog_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_printer_properties_dialog_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with scp_interface_printer_properties_dialog_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type scpinterfacePrinterPropertiesDialogProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfacePrinterPropertiesDialog *
scp_interface_printer_properties_dialog_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG (ret);
  else
    return NULL;
}

/**
 * scp_interface_printer_properties_dialog_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like scp_interface_printer_properties_dialog_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See scp_interface_printer_properties_dialog_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type scpinterfacePrinterPropertiesDialogProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfacePrinterPropertiesDialog *
scp_interface_printer_properties_dialog_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.PrinterPropertiesDialog", NULL);
  if (ret != NULL)
    return SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * scpinterfacePrinterPropertiesDialogSkeleton:
 *
 * The #scpinterfacePrinterPropertiesDialogSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * scpinterfacePrinterPropertiesDialogSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #scpinterfacePrinterPropertiesDialogSkeleton.
 */

struct _scpinterfacePrinterPropertiesDialogSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_scp_interface_printer_properties_dialog_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  scpinterfacePrinterPropertiesDialogSkeleton *skeleton = SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_scp_interface_printer_properties_dialog_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  scpinterfacePrinterPropertiesDialogSkeleton *skeleton = SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_printer_properties_dialog_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_scp_interface_printer_properties_dialog_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  scpinterfacePrinterPropertiesDialogSkeleton *skeleton = SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_printer_properties_dialog_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _scp_interface_printer_properties_dialog_skeleton_vtable =
{
  _scp_interface_printer_properties_dialog_skeleton_handle_method_call,
  _scp_interface_printer_properties_dialog_skeleton_handle_get_property,
  _scp_interface_printer_properties_dialog_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
scp_interface_printer_properties_dialog_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return scp_interface_printer_properties_dialog_interface_info ();
}

static GDBusInterfaceVTable *
scp_interface_printer_properties_dialog_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_scp_interface_printer_properties_dialog_skeleton_vtable;
}

static GVariant *
scp_interface_printer_properties_dialog_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  scpinterfacePrinterPropertiesDialogSkeleton *skeleton = SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_scp_interface_printer_properties_dialog_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _scp_interface_printer_properties_dialog_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _scp_interface_printer_properties_dialog_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _scp_interface_printer_properties_dialog_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.PrinterPropertiesDialog", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
scp_interface_printer_properties_dialog_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_scp_interface_printer_properties_dialog_on_signal_finished (
    scpinterfacePrinterPropertiesDialog *object)
{
  scpinterfacePrinterPropertiesDialogSkeleton *skeleton = SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.PrinterPropertiesDialog", "Finished",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void scp_interface_printer_properties_dialog_skeleton_iface_init (scpinterfacePrinterPropertiesDialogIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (scpinterfacePrinterPropertiesDialogSkeleton, scp_interface_printer_properties_dialog_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (scpinterfacePrinterPropertiesDialogSkeleton)
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG, scp_interface_printer_properties_dialog_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (scpinterfacePrinterPropertiesDialogSkeleton, scp_interface_printer_properties_dialog_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG, scp_interface_printer_properties_dialog_skeleton_iface_init))

#endif
static void
scp_interface_printer_properties_dialog_skeleton_finalize (GObject *object)
{
  scpinterfacePrinterPropertiesDialogSkeleton *skeleton = SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (scp_interface_printer_properties_dialog_skeleton_parent_class)->finalize (object);
}

static void
scp_interface_printer_properties_dialog_skeleton_init (scpinterfacePrinterPropertiesDialogSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = scp_interface_printer_properties_dialog_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG_SKELETON, scpinterfacePrinterPropertiesDialogSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
scp_interface_printer_properties_dialog_skeleton_class_init (scpinterfacePrinterPropertiesDialogSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = scp_interface_printer_properties_dialog_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = scp_interface_printer_properties_dialog_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = scp_interface_printer_properties_dialog_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = scp_interface_printer_properties_dialog_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = scp_interface_printer_properties_dialog_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (scpinterfacePrinterPropertiesDialogSkeletonPrivate));
#endif
}

static void
scp_interface_printer_properties_dialog_skeleton_iface_init (scpinterfacePrinterPropertiesDialogIface *iface)
{
  iface->finished = _scp_interface_printer_properties_dialog_on_signal_finished;
}

/**
 * scp_interface_printer_properties_dialog_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-PrinterPropertiesDialog.top_of_page">org.fedoraproject.Config.Printing.PrinterPropertiesDialog</link>.
 *
 * Returns: (transfer full) (type scpinterfacePrinterPropertiesDialogSkeleton): The skeleton object.
 */
scpinterfacePrinterPropertiesDialog *
scp_interface_printer_properties_dialog_skeleton_new (void)
{
  return SCP_INTERFACE_PRINTER_PROPERTIES_DIALOG (g_object_new (SCP_INTERFACE_TYPE_PRINTER_PROPERTIES_DIALOG_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.fedoraproject.Config.Printing.JobApplet
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:scpinterfaceJobApplet
 * @title: scpinterfaceJobApplet
 * @short_description: Generated C code for the org.fedoraproject.Config.Printing.JobApplet D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-JobApplet.top_of_page">org.fedoraproject.Config.Printing.JobApplet</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.fedoraproject.Config.Printing.JobApplet ---- */

static const _ExtendedGDBusMethodInfo _scp_interface_job_applet_method_info_quit =
{
  {
    -1,
    (gchar *) "Quit",
    NULL,
    NULL,
    NULL
  },
  "handle-quit",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _scp_interface_job_applet_method_info_pointers[] =
{
  &_scp_interface_job_applet_method_info_quit,
  NULL
};

static const _ExtendedGDBusSignalInfo _scp_interface_job_applet_signal_info_finished =
{
  {
    -1,
    (gchar *) "Finished",
    NULL,
    NULL
  },
  "finished"
};

static const _ExtendedGDBusSignalInfo * const _scp_interface_job_applet_signal_info_pointers[] =
{
  &_scp_interface_job_applet_signal_info_finished,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _scp_interface_job_applet_interface_info =
{
  {
    -1,
    (gchar *) "org.fedoraproject.Config.Printing.JobApplet",
    (GDBusMethodInfo **) &_scp_interface_job_applet_method_info_pointers,
    (GDBusSignalInfo **) &_scp_interface_job_applet_signal_info_pointers,
    NULL,
    NULL
  },
  "job-applet",
};


/**
 * scp_interface_job_applet_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-JobApplet.top_of_page">org.fedoraproject.Config.Printing.JobApplet</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
scp_interface_job_applet_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_scp_interface_job_applet_interface_info.parent_struct;
}

/**
 * scp_interface_job_applet_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #scpinterfaceJobApplet interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
scp_interface_job_applet_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * scpinterfaceJobApplet:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-JobApplet.top_of_page">org.fedoraproject.Config.Printing.JobApplet</link>.
 */

/**
 * scpinterfaceJobAppletIface:
 * @parent_iface: The parent interface.
 * @handle_quit: Handler for the #scpinterfaceJobApplet::handle-quit signal.
 * @finished: Handler for the #scpinterfaceJobApplet::finished signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-JobApplet.top_of_page">org.fedoraproject.Config.Printing.JobApplet</link>.
 */

typedef scpinterfaceJobAppletIface scpinterfaceJobAppletInterface;
G_DEFINE_INTERFACE (scpinterfaceJobApplet, scp_interface_job_applet, G_TYPE_OBJECT)

static void
scp_interface_job_applet_default_init (scpinterfaceJobAppletIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * scpinterfaceJobApplet::handle-quit:
   * @object: A #scpinterfaceJobApplet.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-JobApplet.Quit">Quit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call scp_interface_job_applet_complete_quit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-quit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceJobAppletIface, handle_quit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * scpinterfaceJobApplet::finished:
   * @object: A #scpinterfaceJobApplet.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-JobApplet.Finished">"Finished"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("finished",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (scpinterfaceJobAppletIface, finished),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

}

/**
 * scp_interface_job_applet_emit_finished:
 * @object: A #scpinterfaceJobApplet.
 *
 * Emits the <link linkend="gdbus-signal-org-fedoraproject-Config-Printing-JobApplet.Finished">"Finished"</link> D-Bus signal.
 */
void
scp_interface_job_applet_emit_finished (
    scpinterfaceJobApplet *object)
{
  g_signal_emit_by_name (object, "finished");
}

/**
 * scp_interface_job_applet_call_quit:
 * @proxy: A #scpinterfaceJobAppletProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-JobApplet.Quit">Quit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_job_applet_call_quit_finish() to get the result of the operation.
 *
 * See scp_interface_job_applet_call_quit_sync() for the synchronous, blocking version of this method.
 */
void
scp_interface_job_applet_call_quit (
    scpinterfaceJobApplet *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Quit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * scp_interface_job_applet_call_quit_finish:
 * @proxy: A #scpinterfaceJobAppletProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_job_applet_call_quit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with scp_interface_job_applet_call_quit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_job_applet_call_quit_finish (
    scpinterfaceJobApplet *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_job_applet_call_quit_sync:
 * @proxy: A #scpinterfaceJobAppletProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-JobApplet.Quit">Quit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See scp_interface_job_applet_call_quit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
scp_interface_job_applet_call_quit_sync (
    scpinterfaceJobApplet *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Quit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * scp_interface_job_applet_complete_quit:
 * @object: A #scpinterfaceJobApplet.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-fedoraproject-Config-Printing-JobApplet.Quit">Quit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
scp_interface_job_applet_complete_quit (
    scpinterfaceJobApplet *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * scpinterfaceJobAppletProxy:
 *
 * The #scpinterfaceJobAppletProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * scpinterfaceJobAppletProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #scpinterfaceJobAppletProxy.
 */

struct _scpinterfaceJobAppletProxyPrivate
{
  GData *qdata;
};

static void scp_interface_job_applet_proxy_iface_init (scpinterfaceJobAppletIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (scpinterfaceJobAppletProxy, scp_interface_job_applet_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (scpinterfaceJobAppletProxy)
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_JOB_APPLET, scp_interface_job_applet_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (scpinterfaceJobAppletProxy, scp_interface_job_applet_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_JOB_APPLET, scp_interface_job_applet_proxy_iface_init))

#endif
static void
scp_interface_job_applet_proxy_finalize (GObject *object)
{
  scpinterfaceJobAppletProxy *proxy = SCP_INTERFACE_JOB_APPLET_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (scp_interface_job_applet_proxy_parent_class)->finalize (object);
}

static void
scp_interface_job_applet_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
scp_interface_job_applet_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
scp_interface_job_applet_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_scp_interface_job_applet_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], SCP_INTERFACE_TYPE_JOB_APPLET);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, SCP_INTERFACE_TYPE_JOB_APPLET);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
scp_interface_job_applet_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  scpinterfaceJobAppletProxy *proxy = SCP_INTERFACE_JOB_APPLET_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_job_applet_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_job_applet_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
scp_interface_job_applet_proxy_init (scpinterfaceJobAppletProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = scp_interface_job_applet_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, SCP_INTERFACE_TYPE_JOB_APPLET_PROXY, scpinterfaceJobAppletProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), scp_interface_job_applet_interface_info ());
}

static void
scp_interface_job_applet_proxy_class_init (scpinterfaceJobAppletProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = scp_interface_job_applet_proxy_finalize;
  gobject_class->get_property = scp_interface_job_applet_proxy_get_property;
  gobject_class->set_property = scp_interface_job_applet_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = scp_interface_job_applet_proxy_g_signal;
  proxy_class->g_properties_changed = scp_interface_job_applet_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (scpinterfaceJobAppletProxyPrivate));
#endif
}

static void
scp_interface_job_applet_proxy_iface_init (scpinterfaceJobAppletIface *iface)
{
}

/**
 * scp_interface_job_applet_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-JobApplet.top_of_page">org.fedoraproject.Config.Printing.JobApplet</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_job_applet_proxy_new_finish() to get the result of the operation.
 *
 * See scp_interface_job_applet_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
scp_interface_job_applet_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (SCP_INTERFACE_TYPE_JOB_APPLET_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.JobApplet", NULL);
}

/**
 * scp_interface_job_applet_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_job_applet_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with scp_interface_job_applet_proxy_new().
 *
 * Returns: (transfer full) (type scpinterfaceJobAppletProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfaceJobApplet *
scp_interface_job_applet_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SCP_INTERFACE_JOB_APPLET (ret);
  else
    return NULL;
}

/**
 * scp_interface_job_applet_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-JobApplet.top_of_page">org.fedoraproject.Config.Printing.JobApplet</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See scp_interface_job_applet_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type scpinterfaceJobAppletProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfaceJobApplet *
scp_interface_job_applet_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (SCP_INTERFACE_TYPE_JOB_APPLET_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.JobApplet", NULL);
  if (ret != NULL)
    return SCP_INTERFACE_JOB_APPLET (ret);
  else
    return NULL;
}


/**
 * scp_interface_job_applet_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like scp_interface_job_applet_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call scp_interface_job_applet_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See scp_interface_job_applet_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
scp_interface_job_applet_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (SCP_INTERFACE_TYPE_JOB_APPLET_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.JobApplet", NULL);
}

/**
 * scp_interface_job_applet_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to scp_interface_job_applet_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with scp_interface_job_applet_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type scpinterfaceJobAppletProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfaceJobApplet *
scp_interface_job_applet_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SCP_INTERFACE_JOB_APPLET (ret);
  else
    return NULL;
}

/**
 * scp_interface_job_applet_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like scp_interface_job_applet_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See scp_interface_job_applet_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type scpinterfaceJobAppletProxy): The constructed proxy object or %NULL if @error is set.
 */
scpinterfaceJobApplet *
scp_interface_job_applet_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (SCP_INTERFACE_TYPE_JOB_APPLET_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.fedoraproject.Config.Printing.JobApplet", NULL);
  if (ret != NULL)
    return SCP_INTERFACE_JOB_APPLET (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * scpinterfaceJobAppletSkeleton:
 *
 * The #scpinterfaceJobAppletSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * scpinterfaceJobAppletSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #scpinterfaceJobAppletSkeleton.
 */

struct _scpinterfaceJobAppletSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_scp_interface_job_applet_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  scpinterfaceJobAppletSkeleton *skeleton = SCP_INTERFACE_JOB_APPLET_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], SCP_INTERFACE_TYPE_JOB_APPLET);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, SCP_INTERFACE_TYPE_JOB_APPLET);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_scp_interface_job_applet_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  scpinterfaceJobAppletSkeleton *skeleton = SCP_INTERFACE_JOB_APPLET_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_job_applet_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_scp_interface_job_applet_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  scpinterfaceJobAppletSkeleton *skeleton = SCP_INTERFACE_JOB_APPLET_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_scp_interface_job_applet_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _scp_interface_job_applet_skeleton_vtable =
{
  _scp_interface_job_applet_skeleton_handle_method_call,
  _scp_interface_job_applet_skeleton_handle_get_property,
  _scp_interface_job_applet_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
scp_interface_job_applet_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return scp_interface_job_applet_interface_info ();
}

static GDBusInterfaceVTable *
scp_interface_job_applet_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_scp_interface_job_applet_skeleton_vtable;
}

static GVariant *
scp_interface_job_applet_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  scpinterfaceJobAppletSkeleton *skeleton = SCP_INTERFACE_JOB_APPLET_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_scp_interface_job_applet_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _scp_interface_job_applet_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _scp_interface_job_applet_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _scp_interface_job_applet_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.JobApplet", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
scp_interface_job_applet_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_scp_interface_job_applet_on_signal_finished (
    scpinterfaceJobApplet *object)
{
  scpinterfaceJobAppletSkeleton *skeleton = SCP_INTERFACE_JOB_APPLET_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.fedoraproject.Config.Printing.JobApplet", "Finished",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void scp_interface_job_applet_skeleton_iface_init (scpinterfaceJobAppletIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (scpinterfaceJobAppletSkeleton, scp_interface_job_applet_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (scpinterfaceJobAppletSkeleton)
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_JOB_APPLET, scp_interface_job_applet_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (scpinterfaceJobAppletSkeleton, scp_interface_job_applet_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (SCP_INTERFACE_TYPE_JOB_APPLET, scp_interface_job_applet_skeleton_iface_init))

#endif
static void
scp_interface_job_applet_skeleton_finalize (GObject *object)
{
  scpinterfaceJobAppletSkeleton *skeleton = SCP_INTERFACE_JOB_APPLET_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (scp_interface_job_applet_skeleton_parent_class)->finalize (object);
}

static void
scp_interface_job_applet_skeleton_init (scpinterfaceJobAppletSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = scp_interface_job_applet_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, SCP_INTERFACE_TYPE_JOB_APPLET_SKELETON, scpinterfaceJobAppletSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
scp_interface_job_applet_skeleton_class_init (scpinterfaceJobAppletSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = scp_interface_job_applet_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = scp_interface_job_applet_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = scp_interface_job_applet_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = scp_interface_job_applet_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = scp_interface_job_applet_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (scpinterfaceJobAppletSkeletonPrivate));
#endif
}

static void
scp_interface_job_applet_skeleton_iface_init (scpinterfaceJobAppletIface *iface)
{
  iface->finished = _scp_interface_job_applet_on_signal_finished;
}

/**
 * scp_interface_job_applet_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-fedoraproject-Config-Printing-JobApplet.top_of_page">org.fedoraproject.Config.Printing.JobApplet</link>.
 *
 * Returns: (transfer full) (type scpinterfaceJobAppletSkeleton): The skeleton object.
 */
scpinterfaceJobApplet *
scp_interface_job_applet_skeleton_new (void)
{
  return SCP_INTERFACE_JOB_APPLET (g_object_new (SCP_INTERFACE_TYPE_JOB_APPLET_SKELETON, NULL));
}

